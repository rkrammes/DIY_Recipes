"use client";

import TripleColumnLayoutClean from '@/components/TripleColumnLayoutClean';

/**
 * Home Page - KRAFT_AI Terminal interface with three-column layout
 * 
 * This is the main interface with:
 * - Retro terminal header with system metrics
 * - Three-column layout (sections, items, details)
 * - Theme switching (Hackers, Dystopia, Neotopia)
 * - Full terminal-style UI
 */
export default function Home() {
  return <TripleColumnLayoutClean />;
}